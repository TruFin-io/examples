/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const USDC_ACCOUNTING_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [42, 35, 203, 219, 116, 6, 84, 186],
);

export function getUsdcAccountingDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    USDC_ACCOUNTING_DISCRIMINATOR,
  );
}

export type UsdcAccounting = {
  discriminator: ReadonlyUint8Array;
  /**
   * USDC held in the vault's ATA that is immediately available for instant redemptions.
   *
   * Grows when users deposit and when DM returns USDC earmarked for reserve replenishment via
   * `receive_usdc`. Shrinks when users call `instant_redeem` (USDC is paid out immediately) or when
   * `request_redeem` draws down a slow-path redeemer's pro-rata share of the reserve.
   */
  reserve: bigint;
  /**
   * USDC deposited by users but not yet swept to DM for ULTRA conversion.
   *
   * Accumulated by `deposit`; zeroed by `request_ultra_mint` when the operator sends the batch to DM.
   * Can also be drawn down by `request_redeem` to fulfil redemptions without a DM round-trip.
   */
  pendingDeposits: bigint;
  /**
   * USDC that has been transferred to DM via `request_ultra_mint` and is awaiting return as minted ULTRA.
   * Counted as vault assets in `update_total_assets` because the value still belongs to the vault.
   * Decremented by `receive_ultra` (success) or `receive_deposit_refund` (DM rejected the mint).
   */
  sentForMinting: bigint;
  /**
   * USDC committed to slow-path redeemers but not yet paid out via `claim_withdrawal`. Subtracted from
   * `total_assets` in `update_total_assets` to keep the TruBILL share price balanced after shares
   * are burned in `request_redeem`. Decreased when a user calls `claim_withdrawal` to collect their USDC.
   *
   * Sum of `unclaimed_redeems` (USDC owed to open `RedeemRequest` PDAs) plus NAV-difference surplus accrued
   * when DM settled above the NAV credited at `request_redeem` time.
   * The surplus is owner-sweepable via `reconcile_usdc`; `unclaimed_redeems` is the floor.
   */
  owedToUsers: bigint;
  /**
   * Sum of `RedeemRequest.usdc_owed` across all open redeem requests. Subset of `owed_to_users`
   * backing real user claims; floor in `reconcile_usdc` so the owner can sweep only NAV surplus.
   */
  unclaimedRedeems: bigint;
  /**
   * Delta Manager epoch of the last settled ULTRA -> USDC redemption (set by `receive_usdc`).
   * Used by `claim_withdrawal` to decide whether a user's redeem batch has been fulfilled: the user can
   * claim only when `last_settlement_epoch >= earliest_redeem_epoch` on their `RedeemRequest`.
   */
  lastSettlementEpoch: bigint;
  /** Reserved space for future fields. */
  padding: ReadonlyUint8Array;
};

export type UsdcAccountingArgs = {
  /**
   * USDC held in the vault's ATA that is immediately available for instant redemptions.
   *
   * Grows when users deposit and when DM returns USDC earmarked for reserve replenishment via
   * `receive_usdc`. Shrinks when users call `instant_redeem` (USDC is paid out immediately) or when
   * `request_redeem` draws down a slow-path redeemer's pro-rata share of the reserve.
   */
  reserve: number | bigint;
  /**
   * USDC deposited by users but not yet swept to DM for ULTRA conversion.
   *
   * Accumulated by `deposit`; zeroed by `request_ultra_mint` when the operator sends the batch to DM.
   * Can also be drawn down by `request_redeem` to fulfil redemptions without a DM round-trip.
   */
  pendingDeposits: number | bigint;
  /**
   * USDC that has been transferred to DM via `request_ultra_mint` and is awaiting return as minted ULTRA.
   * Counted as vault assets in `update_total_assets` because the value still belongs to the vault.
   * Decremented by `receive_ultra` (success) or `receive_deposit_refund` (DM rejected the mint).
   */
  sentForMinting: number | bigint;
  /**
   * USDC committed to slow-path redeemers but not yet paid out via `claim_withdrawal`. Subtracted from
   * `total_assets` in `update_total_assets` to keep the TruBILL share price balanced after shares
   * are burned in `request_redeem`. Decreased when a user calls `claim_withdrawal` to collect their USDC.
   *
   * Sum of `unclaimed_redeems` (USDC owed to open `RedeemRequest` PDAs) plus NAV-difference surplus accrued
   * when DM settled above the NAV credited at `request_redeem` time.
   * The surplus is owner-sweepable via `reconcile_usdc`; `unclaimed_redeems` is the floor.
   */
  owedToUsers: number | bigint;
  /**
   * Sum of `RedeemRequest.usdc_owed` across all open redeem requests. Subset of `owed_to_users`
   * backing real user claims; floor in `reconcile_usdc` so the owner can sweep only NAV surplus.
   */
  unclaimedRedeems: number | bigint;
  /**
   * Delta Manager epoch of the last settled ULTRA -> USDC redemption (set by `receive_usdc`).
   * Used by `claim_withdrawal` to decide whether a user's redeem batch has been fulfilled: the user can
   * claim only when `last_settlement_epoch >= earliest_redeem_epoch` on their `RedeemRequest`.
   */
  lastSettlementEpoch: number | bigint;
  /** Reserved space for future fields. */
  padding: ReadonlyUint8Array;
};

/** Gets the encoder for {@link UsdcAccountingArgs} account data. */
export function getUsdcAccountingEncoder(): FixedSizeEncoder<UsdcAccountingArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["reserve", getU64Encoder()],
      ["pendingDeposits", getU64Encoder()],
      ["sentForMinting", getU64Encoder()],
      ["owedToUsers", getU64Encoder()],
      ["unclaimedRedeems", getU64Encoder()],
      ["lastSettlementEpoch", getU64Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 128)],
    ]),
    (value) => ({ ...value, discriminator: USDC_ACCOUNTING_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link UsdcAccounting} account data. */
export function getUsdcAccountingDecoder(): FixedSizeDecoder<UsdcAccounting> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["reserve", getU64Decoder()],
    ["pendingDeposits", getU64Decoder()],
    ["sentForMinting", getU64Decoder()],
    ["owedToUsers", getU64Decoder()],
    ["unclaimedRedeems", getU64Decoder()],
    ["lastSettlementEpoch", getU64Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 128)],
  ]);
}

/** Gets the codec for {@link UsdcAccounting} account data. */
export function getUsdcAccountingCodec(): FixedSizeCodec<
  UsdcAccountingArgs,
  UsdcAccounting
> {
  return combineCodec(getUsdcAccountingEncoder(), getUsdcAccountingDecoder());
}

export function decodeUsdcAccounting<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<UsdcAccounting, TAddress>;
export function decodeUsdcAccounting<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<UsdcAccounting, TAddress>;
export function decodeUsdcAccounting<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<UsdcAccounting, TAddress> | MaybeAccount<UsdcAccounting, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getUsdcAccountingDecoder(),
  );
}

export async function fetchUsdcAccounting<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<UsdcAccounting, TAddress>> {
  const maybeAccount = await fetchMaybeUsdcAccounting(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUsdcAccounting<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<UsdcAccounting, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeUsdcAccounting(maybeAccount);
}

export async function fetchAllUsdcAccounting(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<UsdcAccounting>[]> {
  const maybeAccounts = await fetchAllMaybeUsdcAccounting(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeUsdcAccounting(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<UsdcAccounting>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeUsdcAccounting(maybeAccount),
  );
}

export function getUsdcAccountingSize(): number {
  return 184;
}
