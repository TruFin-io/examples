/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SHARE_PRICE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  51, 204, 16, 192, 128, 246, 17, 161,
]);

export function getSharePriceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SHARE_PRICE_DISCRIMINATOR);
}

export type SharePrice = {
  discriminator: ReadonlyUint8Array;
  /**
   * Live TruBILL share price (USDC per TruBILL, scaled by `SCALE_FACTOR`), written each
   * `update_total_assets` as `total_assets / total_shares`. Exposed on this fixed-address
   * account so it's readable without the rotating per-epoch snapshot PDA.
   */
  price: bigint;
  /**
   * DM epoch of the last write. `u64::MAX` until the first `update_total_assets` runs.
   * Staleness check: a consumer compares this against the latest DM epoch.
   */
  lastUpdateEpoch: bigint;
  /** Reserved space for future fields. */
  padding: ReadonlyUint8Array;
};

export type SharePriceArgs = {
  /**
   * Live TruBILL share price (USDC per TruBILL, scaled by `SCALE_FACTOR`), written each
   * `update_total_assets` as `total_assets / total_shares`. Exposed on this fixed-address
   * account so it's readable without the rotating per-epoch snapshot PDA.
   */
  price: number | bigint;
  /**
   * DM epoch of the last write. `u64::MAX` until the first `update_total_assets` runs.
   * Staleness check: a consumer compares this against the latest DM epoch.
   */
  lastUpdateEpoch: number | bigint;
  /** Reserved space for future fields. */
  padding: ReadonlyUint8Array;
};

/** Gets the encoder for {@link SharePriceArgs} account data. */
export function getSharePriceEncoder(): FixedSizeEncoder<SharePriceArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["price", getU64Encoder()],
      ["lastUpdateEpoch", getU64Encoder()],
      ["padding", fixEncoderSize(getBytesEncoder(), 128)],
    ]),
    (value) => ({ ...value, discriminator: SHARE_PRICE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link SharePrice} account data. */
export function getSharePriceDecoder(): FixedSizeDecoder<SharePrice> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["price", getU64Decoder()],
    ["lastUpdateEpoch", getU64Decoder()],
    ["padding", fixDecoderSize(getBytesDecoder(), 128)],
  ]);
}

/** Gets the codec for {@link SharePrice} account data. */
export function getSharePriceCodec(): FixedSizeCodec<
  SharePriceArgs,
  SharePrice
> {
  return combineCodec(getSharePriceEncoder(), getSharePriceDecoder());
}

export function decodeSharePrice<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<SharePrice, TAddress>;
export function decodeSharePrice<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SharePrice, TAddress>;
export function decodeSharePrice<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SharePrice, TAddress> | MaybeAccount<SharePrice, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSharePriceDecoder(),
  );
}

export async function fetchSharePrice<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<SharePrice, TAddress>> {
  const maybeAccount = await fetchMaybeSharePrice(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSharePrice<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<SharePrice, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSharePrice(maybeAccount);
}

export async function fetchAllSharePrice(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<SharePrice>[]> {
  const maybeAccounts = await fetchAllMaybeSharePrice(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSharePrice(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<SharePrice>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeSharePrice(maybeAccount));
}

export function getSharePriceSize(): number {
  return 152;
}
